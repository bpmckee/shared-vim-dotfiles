set history=1000     " Store a lot of history.
set hidden           " Let buffers exist without being in a window.
set autoread         " Reload file changes outside of vim.

" Reduce the update time to decrease noticeable delays (default is 4000ms).
set updatetime=300

" Backspace over indentation, line breaks, and start of start of insert.
set backspace=indent,eol,start

