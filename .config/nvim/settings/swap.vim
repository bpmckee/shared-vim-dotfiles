set noswapfile    " Reuse a buffer, don't create a swap file.
set nobackup      " Don't make a backup before overwriting a file.
set nowritebackup " Remove the backup after a file is successfully written.

